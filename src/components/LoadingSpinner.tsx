import { FC } from 'react'

const LoadingSpinner: FC = () => <div className="h-12 w-12 animate-spin rounded-full border-y-2 border-primary" />
LoadingSpinner.displayName = 'LoadingSpinner'

export { LoadingSpinner }
