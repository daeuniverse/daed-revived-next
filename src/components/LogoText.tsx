import { FC } from 'react'

const LogoText: FC = () => {
  return (
    <h1 className="rounded bg-gradient-to-b from-primary to-secondary bg-clip-text p-1 text-3xl font-extrabold text-transparent outline-0 transition-all duration-500 hover:text-primary focus:ring-2 focus:ring-primary">
      daed
    </h1>
  )
}
LogoText.displayName = 'LogoText'

export { LogoText }
